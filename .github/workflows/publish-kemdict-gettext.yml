name: Publish @kemdict/gettext
on:
  push:
    tags:
      - "v*"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 11
      - uses: actions/setup-node@v6
        with:
          node-version: 26
          cache: "pnpm"
      - name: Install packages
        run: |
          pnpm install
      - name: Build
        run: |
          make build
      - name: Test
        run: |
          make test
      - name: Publish
        run: |
          make publish
